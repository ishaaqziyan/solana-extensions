.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

yesterday.md
work.md

# Secrets — never commit. .env.example is the tracked template.
.env
.env.*
!.env.example

# Throwaway demo-holder keypairs. Devnet only, but still private keys.
.keys

# Indexer's event log — regenerated from webhook deliveries, not source.
indexer/events.jsonl
